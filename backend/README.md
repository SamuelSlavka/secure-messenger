# Flask backend

#### Setup:
    python3 -m venv auth
    source auth/bin/activate
    pip install flask flask-sqlalchemy flask-login

  
#### Starting server:
    flask run
  
 #### Execution:
   http://localhost:5000/<query>
